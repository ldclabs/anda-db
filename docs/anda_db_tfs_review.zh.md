# anda_db_tfs 审查跟进记录（2026-09-06）

[English](anda_db_tfs_review.md)

正确性清单 F01–F10 已全部实现。现有 CBOR 元数据与桶格式保持不变。基线源码为 `63973ca276465f7d480a7fc2176d609f22a96fa7`。

## 已完成的正确性工作

| 编号 | 变更内容 | 回归验证依据 |
| --- | --- | --- |
| F01 | 倒排表归属在桶锁内通过共享下架辅助函数重新校验。新倒排表与桶注册同步发布。 | 重建 token 不变量，不同 ID 的并发清理/插入，flush/重载测试。 |
| F02 | 文档条带跨越成员资格、倒排表与记账；批量 purge 采用有序去重条带。 | 暂停移除时持有条带；并发重插通过搜索与重载验证。 |
| F03 | 泛型 writer 在发布成功前显式执行 flush。 | 底层缓冲写入失败时标记桶为 dirty；重试与重载均通过。 |
| F04 | 文件示例在元数据回调中使用原子暂存/同步/重命名辅助函数。 | 空操作 flush 与桶失败保留旧元数据；部分暂存失败清理临时文件；崩溃遗留文件跳过而不直接删除。 |
| F05 | 严格加载 API 拒绝缺失的 manifest 对象；生产环境 BM25 bootstrap 采用严格加载。部分/失败的加载为只读状态并支持增量恢复。 | 缺失对象集成测试，受保护的部分变更/压缩/flush，中断加载后重试。 |
| F06 | 布尔 OR 合并分别分词的操作数，而非拼接原始字符串。 | RawTokenizer 正确区分 `alpha`、`beta` 与 `alpha beta`。 |
| F07 | 解析时保留操作数大小写。 | 大小写敏感的普通与高级检索结果一致。 |
| F08 | 解析器向严格入口报告组合嵌套预算耗尽。 | 覆盖 0–64 层括号与 0–65 个 NOT 操作符的所有 4,290 种组合。 |
| F09 | 纯元数据访问器保留持久化统计信息；已加载文档计数可通过 `len()` 获取。 | 构造外壳对象后，已持久化的文档总数与平均长度依然存在。 |
| F10 | 可选分词器导入与示例添加 feature 门禁，并在 CI 中测试所有特性组合。 | no-default、default、full、doctests 以及文件示例测试全部通过。 |

基于模型的测试不仅验证召回集合与持久化，还通过独立文档公式比对 BM25 分数。限定候选集的评分保留全局文档频率。所有物化的 NOT 补集（包括相对候选集的补集）均受 10,000 文档防线保护。直接负向倒排表与偶数层 NOT 链避免构造补集。

### 复审跟进修正

- 简单负向操作数直接从评分 map 中剔除倒排 ID。像 `common AND NOT absent1 ...` 这样的链式查询不再针对每个子句复制并全量扫描候选集。
- 混合极性候选表达式应用与顶级 NOT 相同的补集限制。前导 NOT 成对抵消，保留大结果集上的高效双重否定查询。
- 原子文件替换在遇到 `AlreadyExists` 时使用新序号重试，覆盖 PID 复用容器中的崩溃遗留文件。
- 纯元数据外壳对象在报告实时原子搜索计数的同时，保留持久化的文档统计信息。

以上跟进修正已在下方基准表格生成后完成验证。在相同开发机上针对新增 `not_many_absent` 基准进行的专项运行显示，处理 64 个缺失负向子句时，1,000 个文档耗时约 130 µs，10,000 个文档耗时约 865 µs；这接近计算初始公共倒排表的分数成本，而非执行 64 次全量候选集扫描。

## 性能与代码精简

- O01：固定语料索引与多语言分词器基准现已覆盖检索、插入、移除、清除 (purge)、flush、加载、压缩、延迟分位数及写入吞吐。独立进程模式可脱离虚拟对象存储保留上传，直接测量内存峰值。
- O02：每个新词项只放置一次。为当前词项推进满载尾部，而非再次移动所有后续词项。
- O03：查询上下文仅采样一次语料统计，缓存 IDF，预先计算长度归一化因子，并优先处理高选择性 AND 操作数。NOT 过滤器仅计算成员资格。稠密重叠的多词项评分复用首次观察到的文档长度，以增加单次查询 map 为代价减少分片查找；单词项评分不分配该缓存。算术评分前释放 map 读锁。
- O04：Flush 保留 dirty ID/版本与元数据，每次序列化并上传单个桶。在 manifest 提交前不将任何桶标为 clean。
- O05：批量 purge 使用哈希集合维护倒排表成员，并优先探测桶与 ID 交集中较小的一侧。维护性扫描保持单遍遍历倒排表。
- O06：重复出现的词项复用已有词频 map 的 key。内置 Jieba 分词链每次缓冲并排序一个 SimpleTokenizer 跨度。公开的通用归并过滤器保留 API 及全局排序语义；验证完整分词流的一致性。
- C01：变更、查询执行、持久化、压缩及测试拆分为独立的私有模块。共享的下架逻辑防止 remove 与 purge 之间产生逻辑分歧。
- C02：移除未使用的 `anda_db_utils` 依赖，修正版本号、Vec 内部表示、字节长度过滤、并发与装箱 (bin-packing) 文档说明。

## 性能测试数据

测试环境：Apple ARM64 macOS，rustc 1.97.1，工作区 release 配置 (`opt-level = z`，LTO，单个 codegen unit)。默认固定语料使用 1,000/10,000 篇文档及 8 KiB 桶放置目标。表格数据基于保留的基线可执行文件与新版可执行文件背靠背连续运行得出（`paired_before` 基线）；测试期间无并行编译任务。测试数据为本地合成负载测量结果，非生产环境承诺。单个负载的优化不代表所有负载均有收益。

| 基准测试用例 | 基线均值 (µs) | 更新后均值 (µs) | 耗时变化 |
| --- | ---: | ---: | ---: |
| `bm25_search/rare/1000` | 2.729 | 1.825 | -33.1% |
| `bm25_search/common/1000` | 65.707 | 67.394 | +2.6% |
| `bm25_search/and/1000` | 58.107 | 48.859 | -15.9% |
| `bm25_search/not/1000` | 7.088 | 5.171 | -27.0% |
| `bm25_search/or/1000` | 6.801 | 7.272 | +6.9% |
| `bm25_search/rare/10000` | 2.583 | 1.856 | -28.1% |
| `bm25_search/common/10000` | 782.055 | 854.910 | +9.3% |
| `bm25_search/and/10000` | 730.082 | 485.251 | -33.5% |
| `bm25_search/not/10000` | 15.172 | 13.592 | -10.4% |
| `bm25_search/or/10000` | 27.679 | 24.850 | -10.2% |
| `bm25_mutation/new_terms/256` | 1709.366 | 321.365 | -81.2% |
| `bm25_mutation/new_terms/512` | 6347.192 | 645.203 | -89.8% |
| `bm25_mutation/new_terms/1024` | 24158.317 | 1286.401 | -94.7% |
| `bm25_mutation/remove_100` | 3252.975 | 3279.566 | +0.8% |
| `bm25_mutation/purge_100` | 356.625 | 199.059 | -44.2% |
| `bm25_persistence/flush_1000` | 930.156 | 898.116 | -3.4% |
| `bm25_persistence/load_1000` | 1953.336 | 2064.030 | +5.7% |
| `bm25_persistence/compact_1000` | 1316.066 | 1298.208 | -1.4% |
| `bm25_chinese/search_1000` | 270.149 | 183.672 | -32.0% |

预热后的单次调用延迟：

| 文档数 | 查询类型 | p50 (µs) | p95 (µs) |
| ---: | --- | ---: | ---: |
| 1000 | common | 66.083 | 76.333 |
| 1000 | and | 47.916 | 51.791 |
| 10000 | common | 829.042 | 1036.041 |
| 10000 | and | 452.041 | 595.250 |

同版本分词器对比（均使用更新后的词频收集器）：

| 语料 | 通用 Jieba 均值 (µs) | 流式 Jieba 均值 (µs) | 耗时变化 |
| --- | ---: | ---: | ---: |
| english | 1037.429 | 1056.308 | +1.8% |
| chinese | 1489.727 | 1253.911 | -15.8% |
| mixed | 2785.557 | 2322.592 | -16.6% |

部分单词项和稀疏布尔查询仍保留一定开销；这些数据已如实呈现而非刻意隐瞒。独立规范化布尔操作数是保证自定义分词器正确性所必需的。仅在倒排表长度预测存在显著重叠时才启用文档长度缓存，因此互不相交的 OR 列表不会承担无效缓存的额外开销。加载耗时在基线测试中方差较大，不应直接推断为明确的加速。

另一项 20,000 篇文档 / 160,000 个独立词项的独立进程测试丢弃了上传载荷。相同的驱动程序分别链接基线库和更新后的库。子进程 `getrusage().ru_maxrss` 显示 **57,491,456 → 48,693,248 字节**，峰值 RSS 下降约 **15.3%**。两种布局均包含 650 个桶；编码后总数据量约为 4.815 MB，最大的单个桶为 7,427 字节。旧版 flush 在上传前会保留所有序列化的桶缓冲；新版 flush 最多保留一个。RSS 包含索引和分配器开销，因此其降幅不完全等同于数据载荷体积。该数据为单次独立进程运行结果，非统计性延迟对比。

## 存储架构取舍评估（O04、O05、O07）

**高频词项倒排表 (O07)：完成评估；本变更未启用分段。** 在 10,000 篇文档共享词汇表的场景下，尽管设置了 8 KiB 的放置目标，新增一篇文档仍会重写约 580 KB。这证实了文档中记录的软限制与写入放大权衡。仅在内存中做有界 flush 缓冲无法消除该现象。

跨持久化对象对单个词项进行分段会改变现有的“单拥有者倒排表/桶”不变量、加载器对账、删除记账、清单可达性以及废弃对象回收。未来的分段格式应显式对传输表示进行版本控制，支持读取现有全量倒排表，维护逐段的成员资格与脏状态，并支持原子检查点/迁移。其验收测试需覆盖段边界插入/移除/清除、部分/取消写入、缺失段、旧版本迁移以及压缩/重载一致性。应在真实存储后端上对比增量上传字节数与 p95 flush 延迟。本次变更保留现有存储格式，不隐式引入不可控迁移。

**有界并行上传 (O04)：完成评估，未引入默认 API。** 实测后端为内存接收器，数据无法体现上传延迟收益。顺序流式写入提供了内存上限保证，无需改变回调顺序或引入运行时依赖。未来可配置的并发上传器应在具有真实网络延迟的后端上评估，并确保所有上传成功后才提交清单。

**文档反向索引 (O05)：完成评估，未采纳。** 批量哈希集合优化已降低了清除成本。持久化或重新构建的“文档→词项/桶”映射会增加内存消耗，并为每个变更/加载路径增添额外不变量；仅在单文档删除成为典型业务瓶颈时才应采纳。无文本恢复应保持全量扫描，避免依赖其本身可能正在修复的记账状态。

## 复跑测试

```sh
# 在基线版本上使用当前基准源码采集基线
cargo bench -p anda_db_tfs --features full --bench tfs_index -- --save-baseline before
# 在更新后的版本上测量相同语料并对比
ANDA_TFS_METRICS=1 cargo bench -p anda_db_tfs --features full --bench tfs_index -- --baseline before
cargo bench -p anda_db_tfs --features full --bench tfs_tokenizer
# 运行更大规模语料测试（首次运行需保存其自身基线）
ANDA_TFS_BENCH_LARGE=1 cargo bench -p anda_db_tfs --features full --bench tfs_index
# 编译后直接运行 tfs_index 可执行文件，配合
# ANDA_TFS_MEMORY_PROFILE=1 及可选的 ANDA_TFS_PROFILE_DOCS=100000
# 测量进程自身 RSS，而非 cargo/rustc 的内存
cargo bench -p anda_db_tfs --features full --bench tfs_index --no-run
```

`ANDA_TFS_METRICS=1` 会在 1,000 次预热搜索中报告单次调用的 p50/p95，独立于 Criterion 的估计值。内存模式会丢弃上传载荷，而非在内存存储中保留第二份索引副本。`getrusage(RUSAGE_CHILDREN).ru_maxrss` 在 macOS 上单位为字节，在 Linux 上为 KiB；每次测量子进程建议使用全新的父进程。

## 验证

- `cargo test -p anda_db_tfs --no-default-features`
- `cargo test -p anda_db_tfs`
- `cargo test -p anda_db_tfs --all-features`（该版本通过 69 项单元测试、1 项属性测试、10 项集成测试、4 项文档测试）
- `cargo test -p anda_db_tfs --features full --example tfs_demo`
- `cargo clippy -p anda_db_tfs -p anda_db --all-targets --all-features -- -D warnings`
- `cargo test --workspace --all-features`：1530 项测试通过；1 项按既定设置忽略。包含生产环境缺失桶 bootstrap 及存储格式兼容性测试。

工作区中被忽略的测试为生成磁盘固件的专用用例 (`generate_fixture_for_current_version`)，非跳过正确性检查。现有存储格式兼容性测试均已通过。
